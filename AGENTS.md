# AI Qualifier Brief Contract

Scope: this project only.

- This is an incubating lead-intake project and a public GitHub Pages repository.
- Keep the customer-facing copy in clear Russian and avoid promises that a custom voice model will be built from scratch.
- Never add source transcripts, the original DOCX, client personal data, recordings, credentials, tokens, `.env` files, or form-service account details to the repository.
- The browser may contain only a public form endpoint ID. Telegram tokens, email credentials, GitHub tokens, and other bearer capabilities must remain server-side in the selected receiver.
- Preserve device-local draft saving, explicit privacy confirmations, accessible labels, mobile behavior, and reduced-motion handling.
- Verify changes with `npm test` and `npm run pages:build` before publication.
- Do not claim that notifications work until a real test submission is visible in the receiver and its notification channel.
